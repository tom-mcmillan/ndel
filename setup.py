from setuptools import setup

setup(
    name="ndel",
    version="0.1.0",
    py_modules=["ndel"],
    install_requires=[],
    author="Your Name",
    description="Non-Deterministic Expression Language",
)
