// NDEL Demo - Finding promising soccer players
@domain("soccer")

// Query: Find young strikers worth investing in
(position is "striker" || position is "attacking midfielder")
  && age < "young prospect"
  && potential is "high"
  && current_form shows "improving"
  && salary < 50000
  && injury_record is "acceptable"
